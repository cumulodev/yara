MODULE(tests)
MODULE(pe)
MODULE(elf)

// disable math module until I figured out why it won't statically link
// MODULE(math)

#ifdef CUCKOO
MODULE(cuckoo)
#endif

#ifdef MAGIC
MODULE(magic)
#endif

#ifdef HASH
MODULE(hash)
#endif
